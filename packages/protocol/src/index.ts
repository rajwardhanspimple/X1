/**
 * Shared wire and domain types for RE:Arena.
 * Shapes: MatchConfig, InputFrame, RenderSnapshot, StateHash, RunSummary, RunLog,
 * RunSubmission, RunReceipt, LeaderboardPage, ChallengeDescriptor, GhostDescriptor, ProfileView.
 * Populated by WO-19 and later work orders.
 */
export {};
