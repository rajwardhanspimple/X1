# Set in the Vercel project (Settings > Environment Variables). Never commit real values.
# Free tier: Vercel Hobby (1M invocations, 4 CPU-hours per month, non-commercial).
SUPABASE_URL=https://nprfxnegcwqqpjasoxln.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# Shared secret sent by the Supabase Database Webhook in the x-verifier-secret header.
# Same value stored in Supabase Vault as verifier_webhook_secret.
VERIFIER_WEBHOOK_SECRET=
# Where SimContent is fetched from (same host the client uses).
CONTENT_BASE_URL=https://rearena-content.<your-subdomain>.workers.dev
