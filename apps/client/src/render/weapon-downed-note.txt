/**
 * Hide the weapon while the player is down.
 *
 * A downed player was still shown holding a rifle at full presentation, which reads as a rendering fault rather
 * than as death. The view model drops out of sight instead, and returns on respawn.
 *
 * Presentation only. The simulation's weapon state is untouched, so a reload in progress when the player goes down
 * continues to completion exactly as the verifier will replay it.
 */
export function setDowned(downed: boolean): void {
  // implemented in weapon-view.ts
}
